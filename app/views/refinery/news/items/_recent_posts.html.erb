<nav id="recent_news_items">
  <h2><%= t('.recent_posts') %></h2>
  <ul>
    <% @news_items.each do |item| %>
      <li>
        <%= link_to item.title, main_app.news_item_path(item) %>
      </li>
    <% end %>
  </ul>
</nav>